server:
  port: 8080
spring:
  application:
    name: mini-erp
  datasource:
    username: postgres
    password: postgres
    url: jdbc:postgresql://localhost:5432/mini_erp
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true